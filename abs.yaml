# macOS 12.3 or above is required for running the GPU variant (MPS support). No way to specify this for only the GPU
# variant, so it's specified for both.
extra_labels_for_os:
  osx-arm64: [ventura]

task_timeout: 72000

build_env_vars:
  ANACONDA_ROCKET_ENABLE_PY314: yes

# build_parameters:
#  - "--no-test"

channels:
  - https://staging.continuum.io/pbp/fs/triton-feedstock/pr10/b89d14e
